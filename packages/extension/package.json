{
  "name": "@froede/extension",
  "version": "0.3.1",
  "private": true,
  "description": "froede browser extension (Chrome/Edge, Manifest V3). Load dist/ unpacked from chrome://extensions.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-static.mjs"
  }
}
