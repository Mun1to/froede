{
  "name": "froede-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "front + edit + code - point-and-click editing that writes back to your real source code.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Mun1to/froede.git"
  },
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "node scripts/test-unit.mjs && pnpm e2e",
    "unit": "node scripts/test-unit.mjs",
    "e2e": "node scripts/e2e-static.mjs && node scripts/e2e-react.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
